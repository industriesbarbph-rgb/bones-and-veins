{
  "name": "bones-and-veins",
  "version": "1.0.0",
  "private": true,
  "description": "BONES & VEINS — BarbPH SEO Health",
  "type": "module",
  "scripts": {
    "check": "node --check app.js && node --check scripts/scan-health.mjs && node --check scripts/build-ai-package.mjs",
    "scan": "node scripts/scan-health.mjs",
    "ai-package": "node scripts/build-ai-package.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
